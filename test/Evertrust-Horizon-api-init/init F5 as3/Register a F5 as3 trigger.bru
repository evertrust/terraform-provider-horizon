meta {
  name: Register a F5 as3 trigger
  type: http
  seq: 3
}

post {
  url: {{horizonUrl}}/api/v1/triggers
  body: json
  auth: none
}

headers {
  X-API-ID: {{X-API-ID}}
  X-API-KEY: {{X-API-KEY}}
}

body:json {
  {
      "name": "F5as3trigger",
      "type": "f5as3",
      "connector": "{{F5As3Connector}}",
      "retries": 10
  }
}

assert {
  res.body: isJson
  res.status: eq 201
  res.body.name: isDefined
}

script:post-response {
  bru.setEnvVar("F5as3Trigger", res.getBody().name)
}
