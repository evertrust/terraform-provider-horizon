meta {
  name: Register a F5 password credentials
  type: http
  seq: 1
}

post {
  url: {{horizonUrl}}/api/v1/security/credentials
  body: json
  auth: none
}

headers {
  X-API-ID: {{X-API-ID}}
  X-API-KEY: {{X-API-KEY}}
}

body:json {
  {
      "name": "F5bigIpCredentials",
      "type": "password",
      "login": "admin",
      "password": {
          "value": "evertrust123"
      },
      "targets": [
          "mdm",
          "thirdparty"
      ]
  }
}

assert {
  res.status: eq 201
  res.body.name: isDefined
  res.body.type: isDefined
  res.body.type: eq password
  res.body.password: isDefined
}

script:post-response {
  bru.setEnvVar("f5AuthCredentials", res.getBody().name)
}
